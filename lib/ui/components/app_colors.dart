import 'package:flutter/material.dart';

class AppColors {
  // Primary theme color (light brown/beige)
  static const Color primary = Color(0xFFD4A574);

  // Text colors
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.grey;

  // Background colors
  static const Color background = Colors.white;
  static const Color cardBackground = Colors.white;

  // Error color
  static const Color error = Colors.red;

  // Navigation
  static const Color navBackground = Color(0xFFD4A574);
  static const Color navSelected = Colors.white;
  static const Color navUnselected = Colors.white70;
}
